const { chromium } = require('playwright'); (async () => { const browser = await chromium.launch(); const page = await browser.newPage(); const logs = []; page.on('console', msg => logs.push('LOG: ' + msg.text())); page.on('pageerror', err => logs.push('ERR: ' + err.message)); await page.goto('file://' + __dirname.replace(/\\/g, '/') + '/index.html'); await new Promise(r => setTimeout(r, 2000)); require('fs').writeFileSync('pout.txt', logs.join('\n')); await browser.close(); console.log('Done'); process.exit(0); })();
