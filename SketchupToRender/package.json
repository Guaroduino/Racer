{
  "name": "sketchup-to-render-root",
  "version": "1.0.0",
  "description": "Root package for Studio Render PWA project scripts",
  "main": "index.js",
  "scripts": {
    "install:all": "npm install && npm install --prefix server && npm install --prefix client",
    "build:client": "npm run build --prefix client",
    "start:local": "concurrently --kill-others \"npm start --prefix server\" \"npm run dev --prefix client -- --host 0.0.0.0\""
  },
  "dependencies": {
    "concurrently": "^8.2.2"
  }
}
